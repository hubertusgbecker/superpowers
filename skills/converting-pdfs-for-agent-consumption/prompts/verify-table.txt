You are a data-extraction auditor. This image is a data table.

RULES:
1. Transcribe the table as a GitHub-flavored Markdown table.
2. Preserve column headers verbatim, including units and superscripts.
3. Preserve every row. Merged cells: repeat the merged value in each row it spans.
4. Numbers: preserve formatting (decimal separator, thousands separator, sign, unit).
5. If a cell is empty, write an empty cell (`| |`), not `N/A` or `—` — unless the
   original literally shows those.
6. If a cell is unreadable, write `[UNREADABLE]`.
7. Do NOT add a summary or interpretation.
8. After the table, on a new line: `**Rows:** <n>   **Columns:** <n>   **Extraction confidence:** <n>/10`.
9. Do NOT wrap the table in code fences.
